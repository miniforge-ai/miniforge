{:cli/messages
 {:help/title "{binary} - {description}"
  :help/usage "Usage: {binary} <command> [options]"
  :help/command-lines
  ["Commands:"
   "  init [repo-path]    Analyze repo and write .miniforge/config.edn"
   ""
   "  run <file>          Execute a workflow (spec, DAG, or plan file)"
   "    --interactive     Interactive mode (chat-based)"
   "    --worktree <path> Custom worktree path"
   "    --resume <id>     Resume a workflow from its last checkpoint (legacy — prefer `resume`)"
   ""
   "  resume <workflow-id>  Resume a workflow from its last checkpoint"
   "    -q, --quiet       Suppress progress output"
   ""
   "  worktree <subcommand>  Resolve or run inside the active git worktree"
   "    path              Print the resolved git worktree root"
   "    run -- <cmd...>   Run a command inside that worktree root"
   ""
   "  scan [repo-path]    Run compliance scanner against a repository"
   "    -p, --pack <name> Policy pack to use (e.g., foundations-1.0.0)"
   "    -r, --rules <sel> Rule selector: all, always-apply, or rule ID"
   "    -s, --since <ref> Git ref for incremental scanning"
   "    -x, --execute     Apply auto-fixable fixes"
   "    --report          Print full work spec (default: true)"
   ""
   "  status [id]         Show workflow status"
   ""
   "  web                 Start workflow monitoring dashboard (default port: 7878)"
   "    --port N          Port number"
   "    --open            Open browser automatically"
   ""
   "  workflow <subcommand>  Workflow execution"
   "    run <id>          Execute a workflow by ID"
   "      -v, --version   Workflow version (default: latest)"
   "      -i, --input     Input file (EDN or JSON)"
   "      --input-json    Inline JSON input"
   "      -o, --output    Output format: pretty, json, edn (default: pretty)"
   "      -q, --quiet     Suppress progress output"
   "    list              List available workflows"
   ""
   "  chain <subcommand>  Chain execution (multi-workflow)"
   "    run <id>          Execute a chain by ID"
   "      -v, --version   Chain version (default: latest)"
   "      -s, --spec      Input spec file (EDN)"
   "      --input-json    Inline JSON input"
   "      -q, --quiet     Suppress progress output"
   "    list              List available chains"
   ""
   "  fleet <subcommand>  Multi-repo management"
   "    start             Start fleet daemon"
   "    stop              Stop fleet daemon"
   "    status            Show fleet status"
   "    tui               Terminal UI (requires standalone TUI package)"
   "    tui               Terminal UI (5 N5 views, requires standalone TUI package)"
   "    web [--port N]    Start web dashboard (default port: 8787)"
   "    add <repo>        Add repo to fleet"
   "    remove <repo>     Remove repo from fleet"
   ""
   "  pr <subcommand>     PR operations"
   "    list              List PRs"
   "    review <url>      Review a PR"
   "    respond <url>     Respond to comments"
   "    merge <url>       Merge a PR"
   ""
   "  config <subcommand> Configuration management"
   "    init              Initialize config file"
   "    list              List all configuration values"
   "    get <key>         Get specific config value"
   "    set <key> <val>   Set config value"
   "    edit              Open config in $EDITOR"
   "    reset             Reset config to defaults"
   "    backends          List available LLM backends with status"
   "    backend <name>    Set LLM backend (shorthand)"
   "    validate          Validate config file"
   ""
   "  control-plane <sub> Agent control plane"
   "    status            Show all agents and their states"
   "    decisions         List pending decisions from agents"
   "    resolve <id> <r>  Resolve a decision (--comment for note)"
   "    terminate <id>    Terminate an agent"
   ""
   "  doctor              Check system health"
   "  version             Show version info"
   "  help                Show this help"]
  :help/note "{binary} (this CLI) uses Babashka for fast startup."
  :help/tui-install "For terminal UI, install: brew install {tui-package} (includes jlink-bundled JVM)"
  :main/unknown-command "Unknown command: {command}"
  :main/did-you-mean "Did you mean:"
  :main/available-commands "Available {scope}commands:"
  :main/run-help "Run '{command}' for more information."
  :doctor/checks
  [["bb" "Babashka" "Required for CLI"]
   ["gum" "Charm Gum" "Required for TUI"]
   ["git" "Git" "Required for version control"]
   ["gh" "GitHub CLI" "Required for PR operations"]
   ["claude" "Claude CLI" "Required for LLM backend"]]
  :doctor/config-exists "Config file exists at {config-path}"
  :doctor/no-config "No config file. Run '{command}'"
  :status/workflow "Status for workflow: {workflow-id}"
  :status/workflow-todo "TODO: Query workflow component"
  :status/all-workflows "All workflows:"
  :status/all-workflows-todo "TODO: List active workflows"
  :workflow-run/usage "Usage: {command}"
  :workflow-run/failed "Workflow execution failed: {error}"
  :chain-run/usage "Usage: {command}"
  :chain-run/failed "Chain execution failed: {error}"
  :web/not-available "Web dashboard not available in this runtime."
  :web/requires-jvm "The dashboard requires JVM components not available in Babashka."
  :web/pr-train-warning "Warning: Could not create PR train manager: {error}"
  :web/repo-dag-warning "Warning: Could not create repo DAG manager: {error}"
  :web/starting "Starting web dashboard on port {port}..."
  :web/opening-browser "Opening browser..."
  :web/press-ctrl-c "Press Ctrl+C to stop"
  :web/press-enter "Press Enter to open in browser, Ctrl+C to stop"
  :web/port-in-use "Port {port} is already in use."
  :web/solutions "Solutions:"
  :web/use-different-port "  1. Use a different port: bb {command}"
  :web/kill-port-header "  2. Kill the process using port {port}:"
  :web/kill-port-command "     lsof -ti:{port} | xargs kill"
  :web/start-failed "Failed to start web dashboard: {error}"
  :tui/not-available "TUI not available in this runtime."
  :tui/requires-jvm "The TUI requires JVM/Lanterna which isn't available in Babashka."
  :tui/install-package "To use the terminal UI, install the separate package:"
  :tui/use-web "Or use the web dashboard instead:"
  :tui/no-standalone-package "This app does not ship a standalone TUI package.\nUse {command} instead."
  :tui/starting "Starting TUI monitor..."
  :tui/watching "Watching {events-dir} for workflow activity"
  :tui/help-hint "Press 'q' to quit, '?' for help"
  :tui/start-failed "Failed to start TUI: {error}"
  :tui/proper-terminal-note "Note: The TUI requires a proper terminal environment."
  :tui/proper-terminal-advice "Try running from Terminal.app or iTerm2, not from an IDE."
  :workflow-runner/header "{display-name} Workflow Runner"
  :workflow-runner/workflow "  Workflow: {workflow-id}"
  :workflow-runner/version "  Version:  {version}"
  :workflow-runner/start "▶ Workflow started"
  :workflow-runner/completed "✓ Workflow completed"
  :workflow-runner/failed "✗ Workflow failed"
  :workflow-runner/phase-started "→ Phase {phase} started"
  :workflow-runner/phase-completed "✓ Phase {phase} {outcome}"
  :workflow-runner/milestone "★ Milestone: {message}"
  :workflow-runner/agent-started "  • Agent {agent} started"
  :workflow-runner/agent-completed "  • Agent {agent} completed"
  :workflow-runner/agent-failed "  • Agent {agent} failed"
  :workflow-runner/agent-status "  • Agent {agent}: {status}"
  :workflow-runner/tool-invoked "  • Tool {tool-id} invoked"
  :workflow-runner/tool-completed "  • Tool {tool-id} completed"
  :workflow-runner/gate-started "  • Gate {gate} running"
  :workflow-runner/gate-passed "  • Gate {gate} passed"
  :workflow-runner/gate-failed "  • Gate {gate} failed"
  :workflow-runner/default-status "working"
  :workflow-runner/summary-success "✓ Workflow completed"
  :workflow-runner/summary-failure "✗ Workflow failed"
  :workflow-runner/metrics "Tokens: {tokens} | Cost: ${cost} | Duration: {duration}"
  :workflow-runner/errors "Errors:"
  :workflow-runner/full-result "Full result:"
  :workflow-runner/load-failed "✗ Failed to load workflow interface"
  :workflow-runner/error "  Error: {message}"
  :workflow-runner/details "  Details: {details}"
  :workflow-runner/cause "  Cause: {cause}"
  :workflow-runner/possible-causes "Possible causes:"
  :workflow-runner/cause-missing-dep "  - Missing dependency in deps.edn: ai.miniforge/workflow"
  :workflow-runner/cause-compile "  - Namespace compilation error in workflow component"
  :workflow-runner/cause-cycle "  - Circular dependency issue"
  :workflow-runner/namespace-help-header "If the namespace doesn't exist:"
  :workflow-runner/namespace-help-dep "  - Check that ai.miniforge/workflow is in your deps.edn"
  :workflow-runner/namespace-help-build "  - Verify the component was built: clojure -M:poly test"
  :workflow-runner/bb-help-header "If running with Babashka (bb):"
  :workflow-runner/bb-help-command "  - Try the JVM version: clojure -M:dev -m ai.miniforge.cli.main workflow run <id>"
  :workflow-runner/debug-header "For debugging:"
  :workflow-runner/debug-command "  - Run with verbose output: bb -e '(requiring-resolve 'ai.miniforge.workflow.interface/load-workflow)'"
  :classified-error/error-prefix "Error: {message}"
  :classified-error/agent-backend-header "⚠️  Agent System Error (Not Your Fault!)"
  :classified-error/task-code-header "❌ Task Code Error"
  :classified-error/external-header "⚠️  External Service Error"
  :classified-error/generic-header "❌ Error"
  :classified-error/task-completed "Your task completed successfully:"
  :classified-error/partial-work "Partial work completed:"
  :classified-error/agent-backend-context-success "This is a bug in Claude Code's agent runtime, not in your task.\nYour work is complete and safe."
  :classified-error/agent-backend-context "This is a bug in Claude Code's agent runtime."
  :classified-error/task-code-context "This is an issue with the code being generated or the task specification."
  :classified-error/external-context "This is not an issue with your code. The external service\nis temporarily unavailable."
  :classified-error/report-prefix "📝 Please report this to "
  :classified-error/recommendation-prefix "🔄 Recommendation: "
  :classified-error/no-retry-prefix "⚙️  "
  :classified-error/retry-task-code "Fix the issue and retry the task."
  :classified-error/retry-external "Wait a few minutes and retry - this is likely transient."
  :classified-error/retry-agent-backend "Try again later after the bug is fixed."
  :classified-error/retry-generic "Check the error and decide if retry is appropriate."
  :classified-error/no-retry-success "No need to retry - your task succeeded."
  :classified-error/no-retry-failure "Report this bug and try again later."

  ;; ── Selector ──────────────────────────────────────────────────────────
  :selector/auto-selected "ℹ️  Auto-selected workflow: {workflow}{confidence-marker}"
  :selector/reason "    Reason: {reason}"
  :selector/override-hint "    Override with :spec/workflow-type in your spec"
  :selector/confidence-medium " (medium confidence)"
  :selector/confidence-low " (low confidence)"
  :selector/reason-multi-phase "Multi-phase implementation with {pr-count} PRs requires comprehensive review"
  :selector/reason-refactoring-stratification "Refactoring with stratification requires comprehensive design review"
  :selector/reason-large-feature "Large feature requires comprehensive workflow phases"
  :selector/reason-bugfix "Bug fix is well-suited for a fast workflow"
  :selector/reason-docs-only "Documentation changes use a fast workflow"
  :selector/reason-unknown "Unknown task type defaults to the app's default workflow"

  ;; ── Run command ───────────────────────────────────────────────────────
  :run/interactive-not-implemented "Interactive mode not yet implemented"
  :run/interactive-coming-soon "Coming soon: conversational workflow execution"
  :run/usage "Usage: {command}"
  :run/file-not-found "File not found: {path}"
  :run/parsing-spec "Parsing workflow spec: {path}"
  :run/invalid-spec "Invalid workflow spec:"
  :run/validation-error "  - {error}"
  :run/running-workflow "Running workflow: {title}"
  :run/detected-format "Detected {format} format: {path}"
  :run/unrecognized-format "Unrecognized file format in: {path}\nExpected :spec/title (spec), :dag-id (DAG), or :plan/id (plan)"
  :run/failed "Failed to run workflow: {error}"
  :run/error-details "  Details: {details}"

  ;; ── Worktree ──────────────────────────────────────────────────────────
  :worktree/root-not-found "No git worktree found from: {path}"
  :worktree/usage "Usage: {command}"
  :worktree/run-usage "Usage: {command}"

  ;; ── Recommender ───────────────────────────────────────────────────────
  :recommender/parse-warning "Warning: Failed to parse LLM response: {error}"
  :recommender/llm-failed-warning "Warning: LLM recommendation failed: {error}"
  :recommender/llm-unavailable "LLM client not available for workflow recommendation"
  :recommender/no-response "LLM did not return a response"
  :recommender/invalid-format "LLM returned invalid recommendation format"
  :recommender/parse-failed "Failed to parse LLM response"
  :recommender/fallback-task-type "Fallback: Selected based on task type {task-type}"
  :recommender/fallback-default "Fallback: Default to app-configured workflow profile"

  ;; ── Artifact ───────────────────────────────────────────────────────────
  :artifact/header "Workflow Artifacts"
  :artifact/directory "  Directory: {dir}"
  :artifact/entry "  {id}  type:{type}  wf:{workflow-id}"
  :artifact/file-entry "  {path}  ({size})"
  :artifact/none "  No artifacts found."
  :artifact/provenance-header "Artifact Provenance: {id}"
  :artifact/provenance-workflow "  Workflow:  {value}"
  :artifact/provenance-phase "  Phase:     {value}"
  :artifact/provenance-agent "  Agent:     {value}"
  :artifact/provenance-commit "  Commit:    {value}"
  :artifact/provenance-created "  Created:   {value}"
  :artifact/provenance-parents "  Parents:"
  :artifact/provenance-parent-entry "    → {id}"
  :artifact/provenance-files "  Files:"
  :artifact/provenance-file-entry "    {path}"
  :artifact/file-header "Artifact: {id}"
  :artifact/file-path "  Path:  {path}"
  :artifact/file-size "  Size:  {size}"
  :artifact/no-provenance "  Note:  provenance-bundle component not available"
  :artifact/not-found "Artifact not found: {id}"
  :artifact/provenance-usage "Usage: {command}"

  ;; ── ETL ───────────────────────────────────────────────────────────────
  :etl/repo-usage "Usage: {command}"
  :etl/invalid-url "Invalid repository URL: {url}\nExpected format: https://github.com/org/repo or git@github.com:org/repo"
  :etl/running "Running ETL on repository: {url}"
  :etl/complete "ETL complete."
  :etl/artifacts-produced "  Artifacts produced: {count}"
  :etl/output-path "  Output: {path}"
  :etl/failed "ETL failed: {error}"
  :etl/clone-failed "Clone failed: {error}"
  :etl/analysis-complete "Repository analysis complete (ETL fallback)."
  :etl/technologies "  Technologies: {value}"
  :etl/git-host "  Git host:     {value}"
  :etl/packs "  Packs:        {value}"
  :etl/install-note "Install the etl-pipe component for full ETL support."
  :etl/analysis-failed "Analysis failed: {error}"

  ;; ── Evidence ──────────────────────────────────────────────────────────
  :evidence/header "Evidence Bundles"
  :evidence/bundle-entry "  {id}  wf:{workflow-id}  ({status})"
  :evidence/none "  No evidence bundles found."
  :evidence/evidence-dir "  Evidence dir: {dir}"
  :evidence/show-usage "Usage: {command}"
  :evidence/show-header "Evidence Bundle: {id}"
  :evidence/show-workflow "  Workflow:  {value}"
  :evidence/show-status "  Status:    {value}"
  :evidence/show-created "  Created:   {value}"
  :evidence/show-artifacts "  Artifacts: {count}"
  :evidence/show-artifact-entry "    • {type} — {id}"
  :evidence/show-phases "  Phases:    {count}"
  :evidence/not-found "Evidence bundle not found: {id}\nRun `{command}` to see available bundles."
  :evidence/export-usage "Usage: {command}"
  :evidence/export-success "Exported evidence bundle: {id}"
  :evidence/export-format "  Format: {format}"
  :evidence/export-path "  Path:   {path}"
  :evidence/export-raw "Exported (raw): {path}"
  :evidence/export-not-found "Evidence bundle not found: {id}"

  ;; ── Fleet ─────────────────────────────────────────────────────────────
  :fleet/starting-daemon "Starting fleet daemon..."
  :fleet/stopping-daemon "Stopping fleet daemon..."
  :fleet/daemon-todo "TODO: Fleet daemon is an enterprise extension point."
  :fleet/status-header "Fleet Status"
  :fleet/repositories "  Repositories: {count}"
  :fleet/active-workflows "  Active Workflows: {count}"
  :fleet/pending-workflows "  Pending Workflows: {count}"
  :fleet/completed "  Completed: {count}"
  :fleet/failed "  Failed: {count}"
  :fleet/add-usage "Usage: {command}"
  :fleet/remove-usage "Usage: {command}"
  :fleet/added "Added {repo} to fleet"
  :fleet/removed "Removed {repo} from fleet"
  :fleet/watch-starting "Starting fleet TUI monitor..."
  :fleet/watch-events-dir "Events dir: {dir}"
  :fleet/watch-fallback "  (fleet-tui not available, using standalone TUI)"
  :fleet/watch-unavailable "TUI not available in this runtime."
  :fleet/watch-use-web "  Start the web dashboard instead: {command}"
  :fleet/prs-header "Fleet PRs"
  :fleet/prs-no-repos "No repos configured."
  :fleet/prs-add-hint "  Add repos with: {command}"
  :fleet/prs-unreachable "    Could not reach repo (check gh auth)"
  :fleet/prs-none "    No open PRs."
  :fleet/prs-risk-high "[HIGH]"
  :fleet/prs-risk-medium "[MED]"
  :fleet/prs-risk-low "[LOW]"

  ;; ── Policy ────────────────────────────────────────────────────────────
  :policy/header "Available Policy Packs"
  :policy/none "  No policy packs found."
  :policy/installed-header "  Installed packs:"
  :policy/no-installed "  No installed packs. (dir: {dir})"
  :policy/builtin-header "  Built-in packs (classpath):"
  :policy/builtin-entry "    {id} (built-in)"
  :policy/show-usage "Usage: {command}"
  :policy/show-header "Pack: {id}"
  :policy/show-version "  Version:     {value}"
  :policy/show-description "  Description: {value}"
  :policy/show-rules-count "  Rules:       {count}"
  :policy/show-always-tag " [always]"
  :policy/not-found "Pack not found: {id}\nRun `{command}` to see available packs."
  :policy/install-usage "Usage: {command}"
  :policy/install-not-found "Pack file not found: {path}"
  :policy/install-invalid "Invalid pack file — must be EDN: {path}"
  :policy/install-success "Installed: {name}"
  :policy/install-location "  Location: {path}"
  :policy/install-pack-id "  Pack ID:  {id}"

  ;; ── Workflow Commands ─────────────────────────────────────────────────
  :workflow-cmd/execute-usage "Usage: {command}"
  :workflow-cmd/status-usage "Usage: {command}"
  :workflow-cmd/status-not-found "No workflow found with ID: {id}"
  :workflow-cmd/status-header "Workflow: {id}"
  :workflow-cmd/status-status "  Status:  {value}"
  :workflow-cmd/status-started "  Started: {value}"
  :workflow-cmd/status-phases "  Phases:"
  :workflow-cmd/status-error "  Error:   {value}"
  :workflow-cmd/cancel-usage "Usage: {command}"
  :workflow-cmd/cancel-success "Cancel signal sent to workflow: {id}"
  :workflow-cmd/cancel-failed "Failed to cancel workflow: {error}"

  ;; ── PR ────────────────────────────────────────────────────────────────
  :pr/no-repos "No repositories specified."
  :pr/no-repos-hint "Use --repo <owner/repo> or add repos with '{command}'"
  :pr/header "PRs for {repo}"
  :pr/no-open "  No open PRs"
  :pr/entry "  #{number} [{state}] {title} ({author})"
  :pr/list-failed "Failed to list PRs: {error}"
  :pr/query-failed "Failed to query GitHub: {error}"
  :pr/review-usage "Usage: {command}"
  :pr/reviewing "Reviewing: {url}"
  :pr/review-todo "TODO: Implement PR review with agent"
  :pr/respond-usage "Usage: {command}"
  :pr/responding "Responding to comments on: {url}"
  :pr/respond-todo "TODO: Implement PR comment response"
  :pr/merge-usage "Usage: {command}"
  :pr/merging "Merging: {url}"
  :pr/merge-todo "TODO: Use gh pr merge"

  ;; PR Monitor
  :pr/monitor-starting "Starting PR monitor for author: {author}"
  :pr/monitor-polling "Polling every {seconds}s in {dir}"
  :pr/monitor-stop-hint "Press Ctrl+C to stop."
  :pr/monitor-stopping "Stopping monitor..."
  :pr/monitor-stopped "Monitor stopped. Evidence: {evidence}"
  :pr/monitor-interval-bounds "Poll interval must be {min}-{max} seconds, got {value}. Using default."
  :pr/monitor-interval-invalid "Invalid poll interval: {value}. Using default."

  ;; ── Observability ─────────────────────────────────────────────────────
  :observability/error-tailing "Error tailing file: {error}"
  :observability/tailing-header "{icon} Tailing {label}: {file-path}"
  :observability/tailing-all-logs "{icon} Tailing ALL workflow logs"
  :observability/tailing-all-events "{icon} Tailing ALL workflow events"
  :observability/aggregated-not-implemented "Aggregated tailing not yet implemented - use specific workflow-id"
  :observability/file-not-found "File not found: {file-path}"
  :observability/no-log-files "No log files found in {dir}"
  :observability/no-event-files "No event files found in {dir}"
  :observability/available-files "Available {label}:"
  :observability/no-files-found "No {label} found"
  :observability/file-entry "  {path} ({size} MB)"
  :observability/file-required "Error: --file required for 'cat' command"
  :observability/cleanup-result "Cleaned up {count} old rotated log files"
  :observability/cleanup-not-available "Cleanup not available"
  :observability/unknown-subcommand "Unknown subcommand: {subcommand}"

  ;; ── Config ────────────────────────────────────────────────────────────
  :config/error-prefix "Error: {message}"
  :config/warning-read-failed "Warning: Failed to read config file: {message}"
  :config/title "Current Configuration:"
  :config/section-llm "LLM Settings:"
  :config/llm-backend "  backend: {backend} ({provider})"
  :config/llm-model "  model: {model}"
  :config/llm-max-tokens "  max-tokens: {max-tokens}"
  :config/llm-timeout "  timeout: {minutes} minutes"
  :config/section-workflow "Workflow Settings:"
  :config/workflow-max-iterations "  max-iterations: {max-iterations}"
  :config/workflow-max-tokens "  max-tokens: {max-tokens}"
  :config/workflow-failure-strategy "  failure-strategy: {failure-strategy}"
  :config/section-artifacts "Artifacts:"
  :config/artifacts-dir "  dir: {dir}"
  :config/section-config-files "Config Files:"
  :config/user-config "  User: {path}"
  :config/user-config-exists " ✓"
  :config/user-config-not-created " (not created)"
  :config/defaults-embedded "  Defaults: <embedded>"
  :config/section-env-overrides "Override with environment variables:"
  :config/env-backend "  MINIFORGE_LLM_BACKEND=openai"
  :config/env-model "  MINIFORGE_LLM_MODEL=gpt-4"
  :config/get-missing-key "Missing key argument"
  :config/get-usage "Usage: {command}"
  :config/get-example "Example: {command}"
  :config/get-not-found "Key not found: {key}"
  :config/set-missing-args "Missing arguments"
  :config/set-usage "Usage: {command}"
  :config/set-example "Example: {command}"
  :config/set-success "✅ Set {key} to {value}"
  :config/set-saved "   Saved to: {path}"
  :config/init-exists "Config already exists at {path}"
  :config/init-modify-hint "Use '{command}' to modify"
  :config/init-created "✅ Created config at {path}"
  :config/init-edit-hint "Edit with: {command}"
  :config/init-view-hint "View with: {command}"
  :config/edit-saved "Config saved: {path}"
  :config/edit-failed "Failed to open editor: {message}"
  :config/reset-warning "This will reset your config to defaults."
  :config/reset-prompt "Continue? (y/N): "
  :config/reset-success "✅ Config reset to defaults"
  :config/reset-cancelled "Cancelled."
  :config/backend-missing "Missing backend argument"
  :config/backend-usage "Usage: {command}"
  :config/backend-available-header "Available backends:"
  :config/backend-item "  {name}"
  :config/backend-detail-hint "Run '{command}' for detailed status."
  :config/backend-set-success "✅ Backend set to {backend}"
  :config/backend-saved "   Saved to: {path}"
  :config/backend-provider "   Provider: {provider}"
  :config/backend-model "   Model: {model} (default)"
  :config/backend-change-model "To change model:"
  :config/backend-change-model-hint "  {command}"
  :config/validate-no-file "Config file does not exist"
  :config/validate-create-hint "Run '{command}' to create: {path}"
  :config/validate-valid "✅ Config file is valid"
  :config/validate-location "   Location: {path}"
  :config/validate-backend-available "   Backend {backend}: {status}"
  :config/validate-invalid "Config file is invalid or unreadable"
  :config/validate-reset-hint "Try resetting: {command}"

  ;; ── Workflow runner (additional) ──────────────────────────────────────
  :workflow-runner/not-found "Workflow '{workflow-id}' not found. Use 'workflow list' to see available workflows."
  :workflow-runner/validation-failed "Workflow validation failed: {errors}"
  :workflow-runner/artifact-store-warning "Warning: Could not create artifact store, running without persistence"
  :workflow-runner/user-specified "ℹ️  Workflow: {workflow-type} [user-specified]"
  :workflow-runner/auto-selected "\nℹ️  Workflow Auto-Selected: {workflow-type}"
  :workflow-runner/auto-selected-reason "   Reason: {reasoning}"
  :workflow-runner/auto-selected-confidence "   Confidence: {confidence}"
  :workflow-runner/auto-selected-override "   Override with :spec/workflow-type in your spec\n"
  :workflow-runner/stopped "Workflow stopped: {error}"
  :workflow-runner/cancelled "Workflow cancelled"
  :workflow-runner/process-terminated "Process terminated"
  :workflow-runner/sandbox-released "🐳 Sandbox container released"
  :workflow-runner/run-error "\n✗ Error: {error}"
  :workflow-runner/no-workflows "No workflows found."
  :workflow-runner/available-workflows "\nAvailable Workflows:"
  :workflow-runner/workflow-type-label ":workflow/type {type}"
  :workflow-runner/list-failed "Failed to list workflows: {error}"
  :workflow-runner/spec-execution-failed "\n❌ Workflow execution failed: {error}"
  :workflow-runner/event-file-failed "Failed to read event file: {error}"
  :workflow-runner/resuming "\n🔄 Resuming workflow {workflow-id}"
  :workflow-runner/previously-completed "   Previously completed: {count} tasks"
  :workflow-runner/recovered-artifacts "   Recovered artifacts: {count}"
  :workflow-runner/no-completed-tasks "   No completed tasks found in event file. Starting fresh run."
  :workflow-runner/chain-header "⛓  Chain: {chain-id}"
  :workflow-runner/chain-description "   {description}"
  :workflow-runner/chain-steps "   Steps: {count}"
  :workflow-runner/chain-completed "✓ Chain completed — {count} steps in {duration}ms"
  :workflow-runner/chain-failed-at "✗ Chain failed at step: {step}"
  :workflow-runner/chain-execution-failed "\n❌ Chain execution failed: {error}"
  :workflow-runner/no-chains "No chains found."
  :workflow-runner/available-chains "\nAvailable Chains:"
  :workflow-runner/chain-steps-label "{steps} step(s)"
  :workflow-runner/list-chains-failed "Failed to list chains: {error}"
  :workflow-runner/chain-step-completed "chain {chain-id} completed"

  ;; control-plane CLI commands
  :cp/dashboard-not-running    "Dashboard not running."
  :cp/dashboard-start-hint     "Start it with: miniforge web"
  :cp/dashboard-try-again      "Then try again."
  :cp/status-header            "Agent Control Plane"
  :cp/total-agents-label       "  Total Agents:      {count}"
  :cp/pending-decisions-label  "  Pending Decisions: {count}"
  :cp/need-attention-label     "  Need Attention:    {count}"
  :cp/by-status-header         "  By Status:"
  :cp/agents-header            "  Agents:"
  :cp/unnamed-agent            "Unnamed"
  :cp/unknown-fallback         "?"
  :cp/agent-line               "    {status} {name} [{vendor}]"
  :cp/no-decisions-msg         "No pending decisions. All agents are autonomous."
  :cp/pending-decisions-header "Pending Decisions ({count})"
  :cp/decision-line            "  {priority} {summary}"
  :cp/decision-context-line   "           {context}"
  :cp/decision-id-label       "           ID: {id}"
  :cp/decision-options-label  "           Options: {options}"
  :cp/resolve-usage            "Usage: miniforge control-plane resolve <decision-id> <resolution>"
  :cp/resolve-usage-comment    "       --comment \"optional comment\""
  :cp/resolve-success          "Decision resolved: {resolution}"
  :cp/resolve-failed           "Failed to resolve decision."
  :cp/terminate-usage          "Usage: miniforge control-plane terminate <agent-id>"
  :cp/terminate-success        "Agent terminated."
  :cp/terminate-failed         "Failed to terminate agent."

  ;; control-plane CLI color scheme (keyword values, not strings)
  :cp/status-colors  {:running :blue :blocked :yellow :failed :red
                       :unreachable :red :completed :green}
  :cp/priority-colors {:critical :red :high :yellow :medium :blue :low :white}}}
