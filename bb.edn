{:min-bb-version "1.3.0"

 :tasks
 {:requires ([babashka.fs :as fs]
             [babashka.process :as p]
             [clojure.string :as str])

  :init (do
          (def staged-files
            (delay
              (-> (p/sh "git" "diff" "--cached" "--name-only" "--diff-filter=ACM")
                  :out
                  str/split-lines
                  (->> (remove str/blank?)))))

          (defn staged-by-ext [ext]
            (->> (deref staged-files)
                 (filter (fn [f] (str/ends-with? f ext)))))

          (defn run! [& args]
            (let [{:keys [exit]} (apply p/sh args)]
              (when-not (zero? exit)
                (System/exit exit)))))

  ;; ──────────────────────────────────────────────────────────────────────────
  ;; Linting & Formatting
  ;; ──────────────────────────────────────────────────────────────────────────

  lint:clj
  {:doc  "Lint Clojure files with clj-kondo"
   :task (let [clj-files (concat (staged-by-ext ".clj")
                                 (staged-by-ext ".cljs")
                                 (staged-by-ext ".cljc")
                                 (staged-by-ext ".edn"))]
           (if (seq clj-files)
             (do
               (println "🔍 Linting" (count clj-files) "Clojure file(s)...")
               (apply run! "clj-kondo" "--lint" clj-files))
             (println "✓ No Clojure files to lint")))}

  lint:clj:all
  {:doc  "Lint all Clojure files in the project"
   :task (do
           (println "🔍 Linting all Clojure files...")
           (run! "clj-kondo" "--lint" "bases" "components" "development/src"))}

  fmt:md
  {:doc  "Format staged Markdown files (lint --fix)"
   :task (let [md-files (staged-by-ext ".md")]
           (if (seq md-files)
             (do
               (println "📝 Formatting" (count md-files) "Markdown file(s)...")
               (apply run! "markdownlint" "--fix" md-files)
               ;; Re-add the fixed files to staging
               (doseq [f md-files]
                 (p/sh "git" "add" f)))
             (println "✓ No Markdown files to format")))}

  fmt:md:all
  {:doc  "Format all Markdown files in the project"
   :task (do
           (println "📝 Formatting all Markdown files...")
           (run! "markdownlint" "--fix" "."))}

  ;; ──────────────────────────────────────────────────────────────────────────
  ;; Testing
  ;; ──────────────────────────────────────────────────────────────────────────

  test
  {:doc  "Run unit tests via Polylith"
   :task (do
           (println "🧪 Running tests via poly test...")
           (run! "clojure" "-M:poly" "test"))}

  test:all
  {:doc  "Run all tests including integration via Polylith"
   :task (do
           (println "🧪 Running all tests via poly test :all...")
           (run! "clojure" "-M:poly" "test" ":all"))}

  ;; ──────────────────────────────────────────────────────────────────────────
  ;; Pre-commit
  ;; ──────────────────────────────────────────────────────────────────────────

  pre-commit
  {:doc     "Run all pre-commit checks (lint, format, test)"
   :depends [lint:clj fmt:md test]
   :task    (println "\n✅ ALL PRE-COMMIT CHECKS PASSED\n")}

  ;; ──────────────────────────────────────────────────────────────────────────
  ;; Hook Installation
  ;; ──────────────────────────────────────────────────────────────────────────

  hooks:install
  {:doc  "Install git pre-commit hook"
   :task (let [hooks-dir ".git/hooks"
               hook-path (str hooks-dir "/pre-commit")
               script    "#!/usr/bin/env bash\nset -e\nbb pre-commit\n"]
           (if (fs/exists? ".git")
             (do
               (fs/create-dirs hooks-dir)
               (spit hook-path script)
               (fs/set-posix-file-permissions hook-path "rwxr-xr-x")
               (println "✅ Installed pre-commit hook at" hook-path))
             (println "❌ No .git directory found. Initialize git first with: git init")))}

  hooks:uninstall
  {:doc  "Remove git pre-commit hook"
   :task (let [hook-path ".git/hooks/pre-commit"]
           (if (fs/exists? hook-path)
             (do
               (fs/delete hook-path)
               (println "✅ Removed pre-commit hook"))
             (println "ℹ️  No pre-commit hook installed")))}}}
