{:generated-by "eval/policy-fidelity/calibrate.clj",
 :model "claude-opus-4-8",
 :trials 3,
 :gate-bar {:max-clean-fp 0, :min-recall 0.8},
 :rules
 #:std{:clojure-exception-handling
       {:recall 0.5,
        :clean-fp 1,
        :gate-ready? false,
        :fp-cases
        [{:fixture "seed/status_pipeline.clj",
          :trial 2,
          :judge-said
          "New code throwing a structured payload should use slingshot throw+ / throw-anomaly!, not plain throw + ex-info, so consumers can dispatch on the payload shape via try+ key-value selectors rather than digging into ex-data."}],
        :fn-cases
        [{:fixture "seed/step_runner.clj", :trial 1}
         {:fixture "seed/order_service.clj", :trial 0}
         {:fixture "seed/order_service.clj", :trial 2}]},
       :config-as-data
       {:recall 1.0,
        :clean-fp 2,
        :gate-ready? false,
        :fp-cases
        [{:fixture "seed/order_service.clj",
          :trial 1,
          :judge-said
          "The loyalty period (90 days) and awarded tier (\"gold\") are operational/business-policy values an operator would tune without a code release — they belong in an .edn config with a schema, not as embedded literals."}
         {:fixture "seed/page_fetcher.clj",
          :trial 2,
          :judge-said
          "Page size is operator-tunable per-deployment configuration (the docstring ties it to the dashboard table height, which differs across deployments). config-as-data: batch/page sizes that an operator would adjust without a code release belong in an .edn config (with a Malli schema); the in-code def should be the config-absent fallback resolved via (get config :.../page-size default-page-size), not the active value used directly. As written this is the anti-pattern 'magic number extracted to a def when the value should have been EDN.'"}],
        :fn-cases []},
       :exceptions-as-data
       {:recall 0.889,
        :clean-fp 1,
        :gate-ready? false,
        :fp-cases
        [{:fixture "seed/payload_intake.clj",
          :trial 2,
          :judge-said
          "Catch-and-convert of a thrown anomaly into a return value inside a non-boundary namespace. eval.seed.payload-intake matches none of the boundary patterns (*.cli.*, *.boundary.*, *.http.*, *.web.*, *.mcp.*, *.consumer.*/*.listener.*), so per exceptions-as-data the throw/catch should not live here: `process` must return the :transient anomaly as data and `ingest` should inspect it with a predicate, or the conversion must happen at a designated boundary via boundary/execute-with-exception-handling. The docstring's 'at the boundary' refers to input validation, not exception-to-result conversion, so it does not exempt this site."}],
        :fn-cases [{:fixture "seed/step_runner.clj", :trial 1}]},
       :localization
       {:recall 0.917,
        :clean-fp 0,
        :gate-ready? true,
        :fp-cases [],
        :fn-cases [{:fixture "seed/status_pipeline.clj", :trial 2}]},
       :named-constants
       {:recall 1.0,
        :clean-fp 0,
        :gate-ready? true,
        :fp-cases [],
        :fn-cases []},
       :no-dead-code
       {:recall 1.0,
        :clean-fp 0,
        :gate-ready? true,
        :fp-cases [],
        :fn-cases []},
       :result-handling
       {:recall 1.0,
        :clean-fp 0,
        :gate-ready? true,
        :fp-cases [],
        :fn-cases []},
       :self-documenting-code
       {:recall 1.0,
        :clean-fp 0,
        :gate-ready? true,
        :fp-cases [],
        :fn-cases []},
       :validation-boundaries
       {:recall 0.667,
        :clean-fp 0,
        :gate-ready? false,
        :fp-cases [],
        :fn-cases [{:fixture "seed/row_aggregator.clj", :trial 1}]}}}
