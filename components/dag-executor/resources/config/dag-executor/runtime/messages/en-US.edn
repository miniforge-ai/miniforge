{:dag-executor.runtime/messages
 {:descriptor/unknown-kind
  "Unknown runtime kind: {kind}. Known kinds: {known}."

  :descriptor/unsupported-kind
  "Runtime kind {kind} is not yet supported. Supported runtimes: {supported}."

  ;; Runtime selection (N11-delta §3)
  :selector/explicit-unsupported      "Runtime kind {kind} is not supported."
  :selector/explicit-unavailable      "Runtime {kind} is configured but unavailable."
  :selector/none-available            "No OCI-compatible container runtime is available."
  :selector/executable-requires-kind  "An :executable/:docker-path override requires an explicit :runtime-kind."

  ;; OCI runtime adapter diagnostics
  :oci/acquire-failed        "OCI runtime acquire failed"
  :oci/acquire-deadline      "[oci-cli] acquire-environment! deadline {ms}ms exceeded; cancelling runtime subprocess."
  :oci/acquire-timeout       "OCI runtime acquire-environment! exceeded {ms}ms — likely a stuck daemon or image pull"
  :oci/command-interrupted   "Runtime command interrupted"
  :oci/command-timeout       "Runtime command exceeded {ms}ms deadline — process destroyed"
  :oci/command-timeout-log   "[oci-cli] runtime command timeout {ms}ms exceeded; destroying process tree."
  :oci/command-failed        "Command failed"
  :oci/command-failed-detail "{prefix}: {command}{stderr}"
  :oci/bootstrap-failed      "Workspace bootstrap failed"

  ;; State management
  :state/task-not-found          "Task {task-id} not found"
  :state/task-invalid-transition "Cannot transition from {from-status} to {to-status}"

  ;; State management — log diagnostics (developer-facing)
  :state/log-task-updated       "Task updated"
  :state/log-transitioned       "Task status changed"
  :state/log-not-found          "Task not found"
  :state/log-transition-invalid "Invalid task transition"
  :state/log-merged             "Task merged successfully"
  :state/log-completed          "Task completed successfully"
  :state/log-failed             "Task failed"
  :state/log-metrics-updated    "Run metrics updated"}}
