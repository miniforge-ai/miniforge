{:dag-id "dogfood-2026-02-06"
 :description "Miniforge working on itself - task-executor improvements"
 :tasks
 [{:task/id "task-1"
   :task/deps #{}
   :description "Wire in actual LLM backend to generate.clj"
   :files ["components/task-executor/src/ai/miniforge/task_executor/generate.clj"]
   :acceptance-criteria "Replace mock implementation with actual loop/run-simple integration that calls LLM backend"
   :context "Currently generate.clj has a TODO to wire in the actual LLM backend. The inner generate-fn returns mock data instead of calling the LLM backend parameter."}

  {:task/id "task-2"
   :task/deps #{"task-1"}
   :description "Add integration tests for runner and orchestrator"
   :files ["components/task-executor/test/ai/miniforge/task_executor/runner_test.clj"
           "components/task-executor/test/ai/miniforge/task_executor/orchestrator_test.clj"]
   :acceptance-criteria "Create working integration tests with proper mocking that verify task execution flow and concurrency"
   :context "We removed complex integration tests during PR creation due to mock setup issues. Need proper tests that verify runner executes tasks through full lifecycle and orchestrator manages concurrent execution."}

  {:task/id "task-3"
   :task/deps #{"task-1"}
   :description "Add task execution metrics collection"
   :files ["components/task-executor/src/ai/miniforge/task_executor/runner.clj"]
   :acceptance-criteria "Collect and report detailed metrics: tokens used, cost, iterations, time-to-merge, CI runs, review cycles"
   :context "Runner already has update-task-metrics! call but needs to collect comprehensive metrics during execution for observability."}]}
